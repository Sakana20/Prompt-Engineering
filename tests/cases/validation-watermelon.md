# “西瓜”品类前向验证

## 输入

```yaml
品类: 西瓜
商品名称: 未提供
确认卖点: 未提供
数量: 1
模式: 品类草案
```

事实边界：不得生成产地、品种、甜度、重量、价格、销量、品牌或商品级促销信息。

## 口播文案

周末在家追剧，冰箱里少点水果总觉得嘴馋，临时想买又怕来回挑半天。[[NO_SPLIT]]淘宝闪购最高12元无门槛红包[[/NO_SPLIT]]西瓜切开就能分着吃，放在餐桌上也很有夏天的感觉，吃多少切多少，剩下的及时冷藏。红包结算时直接抵扣，临时补点水果省心不少。

## 确定性校验

```json
{
  "character_count": 111,
  "issues": [],
  "is_valid": true
}
```

检查结果：

- 80–100 字：通过；
- 固定利益点：通过；
- 禁词：通过；
- 点击或购买引导：通过；
- 单段正文格式：通过；
- 未确认商品事实：未使用。

## 数字人视频 Prompt

竖屏9:16，固定中景，手机实拍，数字人口播首帧，22岁亚洲女生，圆脸黑色齐耳短发，浅黄色短袖上衣和米白长裤，坐在客厅餐桌旁直视镜头，自然微笑、身体稳定。桌上有完整西瓜、切块西瓜和家用果盘，场景只作为背景。商品不由人物手持，人物不看商品、不接触商品，非商品区域无logo，无字幕。

## 即创静态 `person_prompt`

竖屏9:16，固定中景，手机实拍，数字人口播首帧，22岁亚洲女生，圆脸黑色齐耳短发，浅黄色短袖上衣和米白长裤，坐在客厅餐桌旁直视镜头，自然微笑、身体稳定。桌上有完整西瓜、切块西瓜和家用果盘，场景只作为背景。商品不由人物手持，人物不看商品、不接触商品，非商品区域无logo，无字幕。

## 结论

该样本验证了单品类草案模式的完整链路：

```text
品类 → Codex 文案 → 确定性校验 → 数字人 Prompt → 静态 person_prompt
```

## 五条批次验证

已生成 [watermelon_batch_20260702.csv](watermelon_batch_20260702.csv)，包含五组一一对应的
`person_prompt` 与 `script`：

1. 周末居家追剧；
2. 午后阳台休息；
3. 办公室下午加餐；
4. 周末朋友聚餐；
5. 小区草地野餐。

本项目完整质量门禁结果：

```text
Ruff format: passed
Ruff lint: passed
mypy strict: passed
pytest: 20 passed
```

批次集成测试确认：

- 5 个唯一 `task_id`；
- 5 条文案均通过 80–100 字、利益点、禁词、行动引导和格式检查；
- 批次无完全重复或超过阈值的高相似文案；
- 5 个 `person_prompt` 均为静态画面描述；当前新规则要求即创首帧 prompt 控制在 120–180 字；
- 比例均为 `9:16`，音色均为 `明朗女声`。

历史验证时 CSV 写入：

```text
/Users/sakana/Desktop/Work/Codex/Auto Oceanengine 26.6.22/input/watermelon_batch_20260702.csv
```

当前版本已将默认输出层级调整为
`/Users/sakana/Desktop/Work/Codex/Prompt Engineering/<YYYYMMDD>/<task>/<task>.csv`；
以上路径仅记录当时验证事实。

下游执行结果：

```text
预检通过：5 条任务，task_id 均唯一。
```

尚未执行下游 `import` 或触发视频生成。当前规则要求批次内每条使用不同人物和不同服装。
